-- Codigo Lua gerado automaticamente pelo compilador C para Lua

function dobrar(x)
    return (x * 2)
end

function triplicar(x)
    return (x * 3)
end

function main()
    local a = dobrar(5)
    local b = triplicar(a)
    return 0
end

main()
