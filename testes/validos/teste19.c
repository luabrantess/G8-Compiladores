// Teste 19: Operadores logicos e relacionais em condicional
// Entrada valida em C reduzido.

int main() {
    int a = 1;
    int b = 0;

    if (a > 0 && b != 1) {
        int c = a + b;
        return c;
    }

    return b;
}

// Saida esperada:
// Iniciando analise sintatica...
// Analise concluida com sucesso!
// Codigo Lua gerado em: saida.lua
